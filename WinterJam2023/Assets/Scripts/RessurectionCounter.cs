using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RessurectionCounter : MonoBehaviour
{
    public float uses;

    // Start is called before the first frame update
    void Start()
    {
        uses = 0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
